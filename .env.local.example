# Get a free key at https://web3forms.com — required for the contact form to send.
NEXT_PUBLIC_WEB3FORMS_KEY=

# Classic PAT with `read:user` scope, from https://github.com/settings/tokens
# — powers the GitHub activity graph. Optional; without it the section shows
# a fallback panel instead of the contribution grid.
GITHUB_TOKEN=
